import React from 'react'
import ReactDOM from 'react-dom/client'
import Root from './api project/Root'
import './api project/style.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
  <Root />
  </React.StrictMode>,
)
